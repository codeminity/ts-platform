# @codeminity/ui-kit-vue

## 0.0.1

### 🚀 Features

- Initial release: `CdmtInput` (real `v-model` support) and `CdmtButton` (typed prop pass-through, slot forwarding) — Vue wrappers around `@codeminity/ui-kit-core`'s custom elements. `vue` is a peer dependency.

### Dependency Updates

- @codeminity/ui-kit-core@0.2.0
